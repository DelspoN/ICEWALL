
service pwn0
{
    disable = no
    socket_type = stream
    protocol    = tcp
    wait        = no
    user        = pwn0
    bind        = 0.0.0.0
    server      = /home/pwn0/pwn0
    type        = UNLISTED
    port        = 1000
}
