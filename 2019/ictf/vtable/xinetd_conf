service vtable
{
    disable = no
    socket_type = stream
    protocol    = tcp
    wait        = no
    user        = vtable
    bind        = 0.0.0.0
    server      = /home/vtable/vtable
    type        = UNLISTED
    port        = 1000
}
